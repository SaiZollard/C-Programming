#include<stdio.h>

int main(){
    int side;
    printf("Enter the value of side of the square: ");
    scanf("%d", &side);
    printf("The value of the area is %f", pow(side,2));

    return 0;
    }
