//Write a c program to calculate area of a Rectangle without user input
#include <stdio.h>
int main(){
    int length=3,breadth=8;
    int area=length*breadth;
    printf("The area of the rectangle is %d",area);
    return 0;
}